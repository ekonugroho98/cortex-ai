# ConfigMap for CortexAI
apiVersion: v1
kind: ConfigMap
metadata:
  name: cortex-ai-config
  namespace: cortex-ai
data:
  # Application Configuration
  GCP_PROJECT_ID: "gen-lang-client-0716506049"
  FASTAPI_ENV: "production"
  FASTAPI_HOST: "0.0.0.0"
  FASTAPI_PORT: "8000"
  API_V1_PREFIX: "/api/v1"
  BIGQUERY_LOCATION: "US"
  LOG_LEVEL: "INFO"
  CORS_ORIGINS: '["*"]'
